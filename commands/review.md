---
description: Save what we discussed — update your financial profile with data from this session
allowed-tools: Read, Write, Edit, Bash, Grep, Glob
argument-hint: [optional: "just log it" to skip profile updates]
---

Run the session-review skill to capture data from this conversation.

Scan the conversation for new financial data points, show the user what was learned, and with their permission, update the profile files.

If the user says "just log it" — only append to history.md without updating other profile files.

$ARGUMENTS
